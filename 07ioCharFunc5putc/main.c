#include <stdio.h>

int main()
{
    //Enables to write a single character to a file (or to stdout)
    //function structure int putc(int char, FILE*fp)
    //??????????????????????????????????????
    //HOW TO USE IT?????????????????????????

    ///With FILES

    FILE *fp;
    int ch;

    fp = fopen("someFile.c","w");
    //Putting every character in ASCII from 33 and 100
    for(ch = 33; ch <= 100; ch++)
    {
        putc(ch, fp);
    }
    fclose(fp);

    ///With STDOUT
    /*char ch = 'd';
    putc(ch, stdout);*/


    return 0;
}
